/*package lesson6;

import java.util.ArrayList;
import java.util.function.Predicate;

public class removeIf {
    ArrayList<String> list = new ArrayList<>();
    list.add(null);
    list.add("hi");
    list.add(null)
            list.add("jane");


    void removeIf(list, Predicate<String> f) {
        for (e) {
            if (f.test(e)) list.remove(e);
            //template method pattern 주요한 로직은 원래 애가 하고 일부만 다른 로직이 함. 전체적 로직은 removeif가 하구 있음
            //지울까말까 하는 건 외부 아이가 함

        }
    }

    String a = "hello"
    Arrays.sort(strs,a::compare)

    int action(String a, String b){
        return 1;
    }
    void run(){
        //this는 여기~
        Array.sort(strs, this::action);
        Array.sort(strs, (x,y)->this.action(x,y)); //이 중첩 클래스의 this가 아니고 감싸고 있는 클래스의 this.
    }
}*/
